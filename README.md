# Telegram Joke Bot 🤖

Telegram бот для администрирования групп с автоматическими шутками, системой званий и статистикой пользователей. Тематика сообщества: секретные агенты и IT собеседования.

**Архитектура проекта**: Clean Architecture + Dependency Injection + Event-driven

## Возможности

- 🎭 **Автоматические шутки** — бот автоматически отправляет шутки в группу через заданный интервал
- 📊 **Статистика пользователей** — отслеживание активности каждого участника чата
- 🏆 **Рейтинги** — топ самых активных пользователей
- 🕵️ **Система званий** — автоматическое присвоение званий на основе активности
- ⚙️ **Администрирование** — полный контроль над функциями бота
- 💾 **SQLite база данных** — надёжное хранение всех данных
- 🔧 **Настраиваемые интервалы** — гибкая настройка частоты шуток
- 🔔 **Уведомления** — отправка сообщений с невидимым текстом (spoiler)
- 😄 **Система реакций** — автоматические реакции на сообщения (стикеры/текст)
- 🎯 **Две категории званий** — агентские и IT-специалистов
- 🐳 **Docker поддержка** — готовые образы для контейнеризации

## Технологии

- **Node.js** (ES Modules)
- **node-telegram-bot-api** — работа с Telegram Bot API
- **better-sqlite3** — синхронная работа с SQLite
- **dotenv** — управление переменными окружения
- **Docker & Docker Compose** — контейнеризация

## Архитектура

Проект построен на **Clean Architecture** с использованием **Dependency Injection** и **Event-driven** подхода.

### Структура слоёв

```
src/
├── domain/                 # Бизнес-логика (Core)
│   ├── entities/           # Сущности домена
│   ├── valueObjects/       # Объекты-значения
│   └── events/            # Доменные события
├── application/            # Приложение (Use Cases)
│   └── useCases/          # Сценарии использования
├── infrastructure/          # Инфраструктура
│   ├── repositories/        # Репозитории (Data Access)
│   ├── di/                # Dependency Injection
│   └── database/           # База данных
├── presentation/           # Презентация
│   └── handlers/          # Обработчики событий
├── config/                # Конфигурация
├── constants/             # Константы
├── utils/                 # Утилиты
└── data/                  # Данные (jokes)
```

Подробное описание архитектуры смотрите в [`ARCHITECTURE.md`](ARCHITECTURE.md:1)

### Преимущества архитектуры

- ✅ **Масштабируемость** — легко добавлять новые функции
- ✅ **Тестируемость** — каждый слой тестируется отдельно
- ✅ **Поддерживаемость** — чёткое разделение ответственности
- ✅ **Гибкость** — легко менять реализации без изменения бизнес-логики
- ✅ **Надёжность** — слабая связанность через DI и Events

## Установка

### Способ 1: Локальная установка

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd telegram-joke-bot
```

#### 2. Установка зависимостей

```bash
npm install
```

#### 3. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

#### 4. Настройка переменных окружения

Скопируйте файл примера:

```bash
cp .env.example .env
```

Отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_CHAT_ID=your_admin_chat_id_here
JOKE_INTERVAL_MINUTES=30
DATABASE_PATH=./data/bot.db
```

**Получение ADMIN_CHAT_ID:**

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение боту
3. Скопируйте полученный `Id`

#### 5. Добавление бота в группу

1. Откройте настройки вашей группы
2. Перейдите в "Участники" → "Добавить участника"
3. Найдите вашего бота по имени и добавьте его
4. **Важно:** Дайте боту права администратора для полноценной работы

#### 6. Запуск бота

```bash
npm start
```

Для разработки с автоматическим перезапуском:

```bash
npm run dev
```

### Способ 2: Docker (рекомендуется для продакшена)

#### 1. Настройка .env файла

Создайте и настройте `.env` файл как описано выше.

#### 2. Сборка Docker образа

```bash
docker build -t telegram-joke-bot .
```

#### 3. Запуск контейнера

```bash
docker run -d \
  --env-file .env \
  -v bot-data:/app/data \
  --name telegram-joke-bot \
  telegram-joke-bot
```

#### 4. Использование Docker Compose (рекомендуется)

```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка
docker-compose up -d --build
```

#### 5. Управление контейнером

```bash
# Просмотр статуса
docker-compose ps

# Перезапуск
docker-compose restart

# Остановка
docker-compose stop

# Удаление volumes
docker-compose down -v
```

### Способ 3: Docker Swarm / Kubernetes

Для деплоя в Docker Swarm или Kubernetes используйте созданный образ:

```yaml
# Пример для Kubernetes
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-joke-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telegram-joke-bot
  template:
    metadata:
      labels:
        app: telegram-joke-bot
    spec:
      containers:
      - name: bot
        image: telegram-joke-bot:latest
        env:
        - name: TELEGRAM_BOT_TOKEN
          valueFrom:
            secretKeyRef:
              name: bot-secrets
              key: token
        - name: DATABASE_PATH
          value: "/app/data/bot.db"
        volumeMounts:
        - name: bot-data
          mountPath: /app/data
      volumes:
      - name: bot-data
        emptyDir: {}
```

## Команды бота

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветственное сообщение с обзором команд |
| `/help` | Подробная справка по всем командам |
| `/joke` | Получить случайную шутку |
| `/joke <категория>` | Шутка из указанной категории |
| `/stats` | Ваша личная статистика |
| `/top` | Топ 10 активных пользователей |
| `/top <N>` | Топ N активных пользователей |
| `/allstats` | Статистика всех пользователей |
| `/summary` | Общая статистика чата |
| `/rank` | Ваше текущее звание |
| `/ranks` | Система званий |

### Команды для администраторов

| Команда | Описание |
|---------|----------|
| `/addjoke <текст>` | Добавить новую шутку в базу |
| `/jokes` | Список всех шуток в базе |
| `/jokestats` | Статистика использования шуток |
| `/jokeson` | Включить автоматические шутки |
| `/jokesoff` | Выключить автоматические шутки |
| `/setinterval <минуты>` | Установить интервал авто-шуток (в минутах) |
| `/addadmin <user_id>` | Добавить пользователя в админы |
| `/removeadmin <user_id>` | Удалить пользователя из админов |
| `/admins` | Список всех админов |
| `/notify <текст>` | Отправить уведомление всем (текст скрыт) |

### Категории шуток

- `programming` — шутки про программирование
- `general` — общие шутки
- `tech` — технологические шутки
- `work` — шутки про работу
- `animals` — шутки про животных
- `life` — жизненные шутки
- `interview` — шутки про IT собеседования
- `agency` — шутки про секретных агентов

## Система званий

Бот автоматически присваивает звания на основе количества сообщений в чате.

### Агентские звания

| Звание | Сообщений | Описание |
|---------|-----------|-----------|
| 🐣 Новичок | 0+ | Только начинаешь свой путь |
| 🎓 Агент-стажёр | 10+ | Показал потенциал |
| 🔫 Младший агент | 50+ | Доказал свою полезность |
| 🕵️ Агент | 100+ | Надёжный член команды |
| 🎖️ Старший агент | 250+ | Опытный профессионал |
| ⭐ Специальный агент | 500+ | Элита агентства |
| 🏆 Легенда агентства | 1000+ | Живая легенда |

### Звания IT-специалистов

| Звание | Сообщений | Описание |
|---------|-----------|-----------|
| 🌱 Junior | 0+ | Начинающий разработчик |
| 💻 Middle | 50+ | Опытный разработчик |
| 🚀 Senior | 150+ | Ведущий разработчик |
| 👑 Tech Lead | 300+ | Технический лидер |
| 🏗️ Architect | 500+ | Архитектор решений |
| 💎 CTO Material | 1000+ | Потенциальный CTO |

## Первичная настройка

После запуска бота выполните следующие шаги:

1. **Добавьте себя в админы:**
   ```
   /addadmin <ваш_user_id>
   ```

2. **Настройте интервал шуток (опционально):**
   ```
   /setinterval 30
   ```

3. **Включите автоматические шутки (опционально):**
   ```
   /jokeson
   ```

## Разработка

### Линтинг

```bash
npm run lint
```

### Добавление новых шуток

Отредактируйте файл [`src/data/jokes.js`](src/data/jokes.js:1) и добавьте новые шутки в массив `jokes`:

```javascript
{
  category: 'programming',
  content: 'Ваша шутка здесь'
}
```

### Добавление новых команд

1. Создайте новый файл в [`src/presentation/handlers/`](src/presentation/handlers/index.js:1)
2. Следуйте существующему паттерну с использованием Use Cases
3. Зарегистрируйте обработчик в [`src/index.js`](src/index.js:1)

### Добавление новых Use Cases

1. Создайте новый файл в [`src/application/useCases/`](src/application/useCases/index.js:1)
2. Зарегистрируйте Use Case в [`src/infrastructure/di/config.js`](src/infrastructure/di/config.js:1)

## Мониторинг и логирование

### Просмотр логов

#### Локально:

```bash
npm start
```

#### Docker:

```bash
docker-compose logs -f telegram-bot
```

#### Docker (без compose):

```bash
docker logs -f telegram-joke-bot
```

### Health Checks

Бот включает встроенный health check. Проверьте статус:

```bash
docker inspect telegram-joke-bot --format='{{.State.Health.Status}}'
```

## Требования

### Локально:

- Node.js >= 18.0.0
- npm >= 9.0.0

### Docker:

- Docker >= 20.10
- Docker Compose >= 2.0

## Безопасность

- Никогда не коммитьте файл `.env` в репозиторий
- Храните токен бота в безопасности
- Ограничьте доступ к админским командам
- Регулярно делайте резервные копии базы данных
- В Docker используйте non-root пользователя
- Используйте Docker secrets для чувствительных данных

## Troubleshooting

### Бот не отвечает на команды

1. Проверьте, что бот запущен (`npm start` или `docker-compose ps`)
2. Убедитесь, что токен в `.env` правильный
3. Проверьте логи на наличие ошибок

### Автоматические шутки не отправляются

1. Убедитесь, что авто-шутки включены (`/jokeson`)
2. Проверьте интервал (`/setinterval`)
3. Убедитесь, что бот имеет права администратора в группе

### Проблемы с базой данных

1. Проверьте права доступа к директории `data/`
2. Удалите файл базы данных и перезапустите бота для пересоздания
3. В Docker проверьте volumes: `docker volume ls`

### Проблемы с Docker

#### Контейнер не запускается:

```bash
# Проверьте логи
docker-compose logs telegram-bot

# Проверьте конфигурацию
docker-compose config

# Пересоберите образ
docker-compose up -d --build
```

#### Проблемы с volumes:

```bash
# Удалите старые volumes
docker volume prune

# Создайте новый volume
docker volume create bot-data
```

### Звания не присваиваются

1. Проверьте, что пользователь отправляет сообщения в чат
2. Убедитесь, что статистика корректно обновляется (`/stats`)
3. Проверьте систему званий (`/ranks`)

## Лицензия

MIT

## Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

## Дополнительная документация

- [`ARCHITECTURE.md`](ARCHITECTURE.md:1) — Подробное описание архитектуры
- [`Dockerfile`](Dockerfile:1) — Конфигурация Docker образа
- [`docker-compose.yml`](docker-compose.yml:1) — Конфигурация Docker Compose
- [`package.json`](package.json:1) — Зависимости проекта
